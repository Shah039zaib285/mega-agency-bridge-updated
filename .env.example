# Server
PORT=3000
NODE_ENV=production

# Admin + auth
ADMIN_SECRET=change_me_admin
JWT_SECRET=change_me_jwt
JWT_REFRESH_SECRET=change_me_refresh

# QR page
QR_PASSWORD=change_me_qr

# Database (optional, for backups)
DATABASE_URL=
AUTH_ENC_KEY= # base64 key for AES-256-GCM (required if using backups)

# Logging
LOG_LEVEL=info
SENTRY_DSN=
